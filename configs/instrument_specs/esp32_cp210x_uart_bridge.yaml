schema_version: 1
instrument:
  id: esp32_cp210x_uart_bridge
  kind: uart_bridge
  capabilities: [uart_txrx]
  actions:
    - name: uart_wait_for
      backend: pyserial
      params: [port, baudrate, pattern, timeout]
